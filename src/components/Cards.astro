---
import styles from '../styles/Cards.module.css'
import Card from './Card.astro';
import type { Props as CardProps } from './Card.astro';

export interface Props {
    cards: CardProps[]
}

const { cards } = Astro.props;
---
<ul class={styles.cards}>{cards.map(card => (
    <Card {...card} />
))}</ul>